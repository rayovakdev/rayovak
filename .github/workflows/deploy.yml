name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to production
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy (placeholder)
        run: |
          echo "Deploy target TBD — configure deployment target and credentials here."
          echo "Commit: ${{ github.sha }}"
